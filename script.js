const startButton = $('#start-button');
const stopButton = $('#stop-button');
const resetButton = $('#reset-button');

const secondsTime = $('#seconds');
const minutesTime = $('#minutes')
const hoursTime = $('#hours')



startButton.on('click', startWatch);
stopButton.on('click', stopWatch);
resetButton.on('click', resetWatch);

function startWatch() { // start timer

}

function stopWatch() { // stop timer

}

function resetWatch() { // reset timer

}